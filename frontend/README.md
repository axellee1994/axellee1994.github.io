# Main README for Frontend

- All HTML pages are in `frontend/pages/`
- Global styles in `frontend/assets/`
- Reusable UI components in `frontend/components/`

Update this file as your project grows.
